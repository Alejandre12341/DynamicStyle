# 🧩 Dynamic Style Engine for React Native

Este módulo provee un **motor de estilos y valores dinámicos** para React Native, con soporte para:
- Resolución de expresiones reactivas (`@if`, `@calc`, `@theme`, etc.)
- Evaluación segura de fórmulas matemáticas
- Manipulación de colores (`darken`, `lighten`, `opacity`, `mix`)
- Soporte multiplataforma (`@platform`)
- Herencia de estilos (`@extends`)
- Registro de resolvers personalizados

---

## 🚀 Instalación

Asegúrate de tener instaladas las dependencias necesarias:

```bash
npm install mathjs tinycolor2
